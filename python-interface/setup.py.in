#
# Auto-generated by CMake, do not edit!
#
import setuptools
import shutil
import os

if not os.path.isfile("$<TARGET_FILE:spncpy>"):
    raise RuntimeError("Python interface library not found, run make first")

targetFile = os.path.join(os.path.realpath(os.path.dirname(__file__)), "spncpy", "$<TARGET_FILE_NAME:spncpy>")
print(f"Copying Python interface library $<TARGET_FILE:spncpy> to {targetFile}")
shutil.copyfile("$<TARGET_FILE:spncpy>", targetFile)

with open("requirements.txt", "r") as rf:
    requirements = rf.readlines()

setuptools.setup(
    name="spnc",
    version="0.0.1",
    author="Embedded Systems and Applications Group, TU Darmstadt",
    author_email="sommer@esa.tu-darmstadt.de",
    description="SPNC: A CPU- and GPU compiler for Sum-Product Networks",
    packages=setuptools.find_packages(include=["spncpy", "spnc", "spnc.*"]),
    package_data={"spncpy" : ["$<TARGET_FILE_NAME:spncpy>"]},
    include_package_data=True,
    install_requires=requirements,
    setup_requires=["pytest-runner"],
    tests_require=["pytest"],
    test_suite="test",
)